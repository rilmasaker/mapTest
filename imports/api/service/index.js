import { Meteor } from 'meteor/meteor';
import { ServiceConfiguration } from 'meteor/service-configuration';

// const oauthGoogle = Meteor.settings.oauth.google;

// ServiceConfiguration.configurations.upsert(
//   { service: 'google' },
//   { $set: { ...oauthGoogle } }
// );
